# The @deepseek-ai/* peers are supplied by the harness at runtime, exactly as
# the profile directory does it (see a dsh profile's own pnpm-workspace.yaml).
# Auto-installing them would pull unrelated published versions into the build.
auto-install-peers=false
strict-peer-dependencies=false
